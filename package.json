{
  "name": "blaze-sports-intel",
  "private": true,
  "workspaces": [
    "apps/web",
    "infra/cloudflare/workers/dev-ue-proxy"
  ],
  "scripts": {
    "postinstall": "npm install --workspace apps/web --ignore-scripts && npm install --workspace infra/cloudflare/workers/dev-ue-proxy --ignore-scripts",
    "dev:web": "npm run dev --workspace apps/web",
    "build:web": "npm run build --workspace apps/web",
    "deploy:worker": "npm run deploy --workspace infra/cloudflare/workers/dev-ue-proxy"
  }
}
